schema="EIMTRD";
query="select TC_NAME, TS_NAMES from EIMTRD.ETRD_TEST_CASES C INNER JOIN 
(select TC_ID, STRING_AGG(TS_NAME) TS_NAMES from EIMTRD.ETRD_SUITES_CASES SC INNER JOIN EIMTRD.ETRD_TEST_SUITES S 
 ON SC.TS_ID = S.TS_ID GROUP BY TC_ID) I ON C.TC_ID = I.TC_ID";